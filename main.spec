# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[('assets', 'assets'), ('animations', 'animations'), ('boss_five', 'boss_five'), ('boss_four', 'boss_four'), ('boss_one', 'boss_one'), ('boss_three', 'boss_three'), ('headers', 'headers'), ('music', 'music'), ('save_system', 'save_system'), ('sound_board', 'sound_board'), ('sound_effects', 'sound_effects'), ('tutorial', 'tutorial')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='main',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
